# Cardiac VisionAI — VLM Fine-Tuning Dependencies
# =================================================
# Core ML stack
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.34.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0

# Unsloth (NVIDIA path) — install separately:
#   pip install unsloth
# For the latest:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Data processing
datasets>=3.0.0
Pillow>=10.0.0
numpy>=1.24.0

# Medical imaging
nibabel>=5.0.0        # NIfTI file reading (ImageCAS)
pydicom>=2.4.0        # DICOM file reading (Heart CT)
scikit-image>=0.21.0  # Image processing utilities

# Utilities
tqdm>=4.65.0
rich>=13.0.0          # Pretty console output

# Optional: Intel Extension for PyTorch (future Intel VM support)
# pip install intel-extension-for-pytorch
# pip install openvino openvino-dev

# Optional: Weights & Biases for experiment tracking
# pip install wandb
